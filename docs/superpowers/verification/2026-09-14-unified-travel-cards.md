# 统一卡片与演示信息验收

日期：2026-09-14。本轮在独立新版工作树继续修改，基点为 `6d84658`。

## 实现结果

- 单张和多张查询结果统一使用完整差旅单据卡片；每轮最多 3 张及查看全部结果入口继续保留。
- 移除“查看详情”和审批时间线、提交时间、过程标签，只展示当前单据状态及申请内容。系统查看按钮仍为演示入口。
- 默认部门改为“数字化部”，默认付款公司改为“杭州某科技公司”；申请、变更卡片及抽屉标题统一为“付款公司”。旧创建草稿和历史卡片的展示名称同步更新。
- 申请单、变更单及种子样例使用同一递增单号序列，从 `123000001` 起分配。事务内分配避免并发重复；撤回重提、请求重试不产生新号，重启后继续原序列。
- 已有演示单据统一编号并保留旧号别名，查询、详情、办理及回执继续指向相同内部单据 ID；被替代历史版本仍按既有规则隐藏。
- 正式单据、创建草稿和变更草稿的行程段同日只显示出发日期，跨日显示起止日期。
- 输入框上方的继续编辑、待确认操作、提示条和提交结果常驻区已移除。编辑从草稿卡片进入；必要的错误及 UNKNOWN 恢复入口放在实际对话反馈或抽屉内。

## 自动验证

| 范围 | 结果 |
|---|---|
| 后端完整回归 | 232 passed，16.75 秒 |
| 前端完整回归 | 7 个文件，75 passed，2.42 秒 |
| TypeScript 校验及 Vite 构建 | 成功，1938 modules |
| 单号新增回归 | 覆盖申请与变更共享序列、并发唯一分配、重试和重提、重启、旧编号迁移及回执、种子幂等、历史创建卡片与确认指纹保护 |

先观察到旧编号和旧详情交互不符合新要求，再实现修复。完整回归发现旧草稿的部门、公司名称导致表单校验失败，现已在本次编辑校验时采用同一组织的新名称，继续生成新的确认版本。移除提示条后的未知结果反馈也已调整到抽屉内，既有版本冲突和同请求恢复测试通过。

## 真实页面与数据核对

- 更新前已备份新版 SQLite 数据库到私密 QA 目录。30 张已有模拟单据已更新为 9 位数字单号，逐张比较确认行程内容、状态、`tflag`、版本及前后序关系均未改变；下一可分配号为 `123000031`。
- 当前接口返回 26 张可见单据，均为数字单号。旧号 `DEMO-SEED-当前停留` 可查询到 `123000002`。
- 真实 Dify 输入“查看差旅单据123000001”，回复“已为您查询到单据信息，详见下方卡片。”成功返回对应单据。
- 浏览器检查了两张查询结果和一张后续结果：全部使用完整卡片，每张有两段行程；没有 `details` 或审批历史节点；同日段只显示一个日期。
- 页面输入区只剩消息输入框、发送按钮和输入方式说明，没有生命周期常驻工具条。
- 已从编号 `123000002` 的变更草稿卡片点击“编辑变更”，成功打开当前草稿抽屉；取消后草稿保留。
- 旧创建结果卡片的显示编号已核对为 `123000030`，部门和付款公司显示新名称，原确认指纹不变。

真实证据位于 `模拟差旅系统/.local/qa/` 的 `unified-cards-*.json`、`unified-cards-transcript.jsonl` 及更新前数据库备份；不提交 Git。

## 运行与版本保护

原版 `main`／`demo-baseline-20260913` 仍指向 `936c625`，原目录受跟踪源码未改变。本轮只重启新版 8876 服务；网关和隧道保留，原版 8766 与新版 8876 的健康检查均返回 HTTP 200。

本轮沿用独立新版 Dify 第 4 版，没有修改新旧 Dify 节点或原始 DSL。新版入口为 [差旅助手](http://127.0.0.1:8876/assistant)，操作说明见 [V2 使用说明](../../../模拟差旅系统/单据生命周期V2使用说明.md)。
