# 当前差旅 Demo 的 GitHub 交付计划

> **执行方式：** 使用 `superpowers:executing-plans` 在当前已隔离工作树内逐项执行。用户已授权将当前 Demo 推送到指定仓库，无需重复确认执行或推送。

**目标：** 向 `khalilxxxx/autobusinesstrip` 交付当前可部署 Demo、可导入 Dify DSL、最新需求说明及跨电脑部署指南。

**方案：** 保留现有三部分实现和完整源码依赖，只补充公开交付文档及无凭证配置模板。先验证部署说明与仓库内容，再把当前分支推送为远端 `main`，不修改本地原 `main` 或旧 Dify 应用。

**技术栈：** Dify Chatflow、Python / FastAPI / SQLite、React / TypeScript / Vite、Cloudflare Tunnel、Git / GitHub。

**需求依据：** 用户于 2026-09-14 提出的 GitHub 交付要求，以及本任务已确认的最新业务规则和界面修订。

## 全局约束

- 所有交付说明使用中文；代码和技术标识保留英文。
- 只在生命周期 V2 工作树修改；保留原版本提交和 `demo-baseline-20260913` 标签。
- 不改变线上 Dify 应用；公开 DSL 的两个环境变量保持空值。
- 不提交 `.local`、数据库、密钥、依赖目录或真实运行会话。
- 不覆盖未知远端提交；目标仓库已确认是空仓库。

## 任务 1：确认可发布内容

- [x] 核对分支、干净工作树、原版基线和目标远端为空。
- [x] 阅读当前 DSL、运行控制器、配置格式、种子接口与依赖。
- [x] 检查待推送完整历史，识别凭证及不应公开的本机运行文件。

## 任务 2：补齐可阅读、可部署的交付物

**文件：** 新增 `README.md`、`docs/需求说明.md`、`docs/部署指南.md`、两份 `模拟差旅系统/examples/lifecycle-*.example.json`；更新 `AGENTS.md` 和现有新版入口说明。

- [x] 根 README 提供功能、架构分工、DSL 与部署入口。
- [x] 需求说明覆盖已确认状态、tflag、串行变更、生效与作废、查询推理、统一卡片、轮次和回执恢复，并增加 Mermaid 图。
- [x] 部署指南说明 macOS / Linux / Windows WSL2、工具版本、独立 Dify 导入、模型、两份配置、网关与启动、样例生成、验证、停止重启和故障排查。
- [x] 配置模板不包含可用密钥或本机应用 ID；修改入口链接为仓库内相对路径。

## 任务 3：验证交付可复现

- [x] 在临时干净副本中，使用文档命令安装锁定依赖、构建前端；不影响正在运行的 Demo。
- [x] 校验示例配置、DSL 节点/连线/摘要、Markdown 相对链接和 Mermaid 语法。
- [x] 用临时数据库验证后端启动、默认信息、样例初始化及重复初始化行为；运行现有相关测试。
- [x] 记录验证结果及实测边界；真实 Dify 账号和模型凭证由部署者自行配置。

实际验证发现共享创建测试夹具仍写死旧部门和公司，改为读取主数据默认名称后，生命周期 DSL 9 项与共享创建链 55 项全部通过。详见 `docs/发布验证.md`。

## 任务 4：提交与推送

- [x] 提交本轮文档和配置模板，保持本地原 `main` 不变。
- [x] 配置用户指定 remote，推送当前 HEAD 为远端 `main`，并保留原版基线标签。
- [x] 通过 GitHub API 与远端引用核对提交、README、需求说明、部署指南和 DSL。
- [x] 将交付入口和完成情况整理到仓库首页与发布验证文档。

交付仓库：[khalilxxxx/autobusinesstrip](https://github.com/khalilxxxx/autobusinesstrip)。默认分支为 `main`；关键交付物已与 GitHub 文件对象摘要核对一致，原版基线标签也已推送。
