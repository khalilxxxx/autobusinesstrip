# GitHub 交付验证

日期：2026-09-14。交付目标：`https://github.com/khalilxxxx/autobusinesstrip`。当前业务版本以提交 `8521fee` 为基础，本轮补充公开文档、部署配置模板，并修正一处过期测试夹具。

## 交付内容

- 当前 Dify Chatflow DSL、节点源码、提示词、构建脚本和 SHA-256 清单。
- [当前系统需求说明](需求说明.md)，分别说明 Dify、后端 Demo 与 AI 助手，包含状态、查询、变更和回执流程。
- [跨电脑部署指南](部署指南.md)，包含 Dify 导入与发布、模型、应用密钥、网关、安装、样例、启停与问题排查。
- 两份空凭证配置模板；完整前后端代码及创建申请节点依赖。
- 原版基线提交与 `demo-baseline-20260913` 标签保持可定位。

## 本轮实际验证

在不包含原 `.venv`、`node_modules`、`data` 或 `.local` 的临时源码副本中，从锁定依赖重新安装和构建。未停止或改写原电脑正在运行的新旧 Demo。

| 项目 | 结果 |
| --- | --- |
| 环境 | macOS ARM64，Python 3.9.6，Node 22.16.0，npm 10.9.2 |
| Python 锁定依赖安装 | 通过，使用新建虚拟环境 |
| `npm ci` | 通过，使用新的依赖目录 |
| TypeScript / Vite 构建 | 通过，1938 个模块 |
| 后端 `pytest` | 232 项通过，18.17 秒 |
| 前端 Vitest | 7 个文件、75 项通过，2.00 秒 |
| 生命周期 DSL 测试 | 9 项通过，1.812 秒 |
| 共享创建链测试 | 55 项通过，5.086 秒 |
| Mermaid 语法 | 8 张图通过 Mermaid 11.12.2 解析 |
| 部署 Bash 片段 | 16 个片段通过 `bash -n` |
| 文档配置步骤 | 实际执行配置生成片段，控制器成功读取两份配置并生成私密运行 DSL |
| DSL 一致性 | 55 节点 / 93 连线，源文件与产物 SHA-256 均与清单一致 |
| 可分享配置 | 可导入 DSL 两个环境变量为空；JSON 模板无应用 ID、API Key 或 token |

临时数据库和独立空闲端口的 HTTP 验证：

- `/health`、`/assistant`、`/simulator`、`/openapi.json` 返回 200。
- 默认部门为“数字化部”，付款公司为“杭州某科技公司”。
- 首次种子初始化创建 9 张单，单号为 `123000001` 至 `123000009`；再次初始化 `created: 0`。
- 查询包含单据状态、行程和接口 `actions`。
- 验证完成后停止临时进程，原 8876 与 8766 服务健康检查仍为 200。

本轮首次执行 DSL 测试时，创建确认测试因旧夹具写死“演示业务部／演示科技公司”而未通过组织选项校验。定位到 `ORG_UNAVAILABLE` 后，夹具改为读取主数据的默认部门和付款公司；重新执行 9 项生命周期 DSL 测试和 55 项共享创建链测试全部通过。该修改不改变业务代码、公开 DSL 或线上工作流。

## 公开内容核对

- GitHub 默认分支 `main` 已包含当前 Demo；README、需求、部署指南、验证记录、DSL、共享节点、启动脚本和两份模板共 9 项关键交付物的文件对象摘要与本地一致。
- 原版 `demo-baseline-20260913` 标签已推送，远端解引用仍为 `936c625b6627cc17916895cd62998063ea586e1e`。
- Gitleaks 8.30.1 扫描待推送完整提交历史，未发现凭证。
- 额外对历史文件内容匹配本机实际 Dify Key 与网关 token，未发现命中；扫描输出未包含凭证原文。
- `.local`、运行数据库、会话、虚拟环境、前端依赖与构建产物均不进入 Git。
- 本地原 `main` 与原版标签仍指向 `936c625b6627cc17916895cd62998063ea586e1e`。
- 当前 DSL SHA-256：`17f4ac743888d6d9293fcd2958ae11100e9adfef793667e32db4a10e4a5ccc15`。

## 验证边界

本轮干净部署验证覆盖本地依赖、构建、配置、接口和离线工作流契约，未新建另一套付费模型账号或发布另一套线上应用。当前开发环境已有真实 Dify 联调和浏览器验收，见 [统一卡片验收](superpowers/verification/2026-09-14-unified-travel-cards.md) 与 [聚焦对话验收](superpowers/verification/2026-09-13-focused-travel-cards.md)。

Linux / WSL2 安装说明基于当前 POSIX 控制器和官方工具安装方式整理，本轮没有在这些操作系统上运行端到端测试。流程图已通过语法解析，具体显示由 GitHub Markdown 的 Mermaid 渲染器完成。
