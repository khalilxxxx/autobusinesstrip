# 普通员工模拟差旅系统验证记录

当前服务版本为 0.1.1，城市扩充与接口精简的最新验证见文末“2026-09-12 更新验证”。

以下为首版记录。日期：2026-09-11。版本：0.1.0。运行环境：macOS、Python 3.9.6、项目独立虚拟环境。

## 交付状态

服务已启动于 [本地接口调试页](http://127.0.0.1:8766/docs)，健康检查返回 `status=ok`。当前场景为 `SUCCESS`，默认模拟提交成功。停止后可在本项目目录重新执行 `./run.sh`。

## 验证结果

| 验证 | 实际结果 |
|---|---|
| 自动测试 | `.venv/bin/python -m pytest -q --tb=short`：`23 passed in 0.64s` |
| 真实 HTTP 冒烟 | 上下文、21 项交通、multipart 城市查询、成功创建、请求回放、详情、回执、预设失败全部通过；结束后恢复原场景 |
| 浏览器调试 | 本地 Swagger JS/CSS 正常加载；页面健康检查返回 HTTP 200；默认创建示例点击 Execute 后返回 SUCCESS 和模拟单号 |
| 持久化与并发 | 临时 SQLite 测试覆盖服务重建后单据／回执／场景保留，同请求并发只创建一张申请 |
| 代码审阅 | 主体实现及最终文档修正均完成只读审阅，未发现必需修复的问题；审阅测试使用临时数据库 |

浏览器验证发现并修正了两处文档问题：默认响应示例显示成字符串；FastAPI 生成 OpenAPI 时省略了业务必填的 `dqydbg: null`。现已补齐响应示例，并恢复请求、单据查询示例中的原始 JSON。新增“文档默认请求可直接提交”用例，确认修正前失败、修正后通过。

## 保留的演示数据

本次运行验证新增两张成功模拟申请，方便打开申请列表检查。

| 来源 | 模拟单号 | 请求标识 |
|---|---|---|
| HTTP 冒烟 | `DEMO-CL-20260911-4CE02A131D4C` | `smoke-a0cd930f722845a19fcb9d367d80b404` |
| 浏览器默认示例 | `DEMO-CL-20260911-D4676F867A36` | `swagger-check-20260911-01` |

预设失败回执使用请求标识 `smoke-a0cd930f722845a19fcb9d367d80b404-failure`，该请求没有生成成功申请。场景已恢复为成功，重复原请求仍会回放当时的结果。

## 当前范围

固定普通员工和 21 项交通，默认有提单权限。提交业务结果由预设开关决定；只检查请求结构，不计算历史行程冲突、路线或其他实际提单规则。

本次交付为本地模拟 API 与调试入口。AI 助手聊天前端、Dify Chatflow 接入及云端访问本地服务的通道属于后续工作；本次未修改线上 Dify 或公开服务。

启动、接口与示例操作见 [使用说明](/Users/khalil/Documents/智能提单项目/模拟差旅系统/README.md)。

## 2026-09-12 更新验证

版本 0.1.1：新增 45 条国内城市样本，总计 50 条。新增编码采用 `DEMO_` 前缀，原杭州、桐庐编码及昆山到苏州的映射保留。

城市查询的唯一业务参数为表单 `filter`。接口定义、响应示例、调用示例和对接文档已同步精简；城市条目仍保留数据来源和匹配说明。

| 验证 | 实际结果 |
|---|---|
| 自动测试 | `.venv/bin/python -m pytest -q --tb=short`：`23 passed in 0.64s`；更新后的城市参数用例先失败、实现后通过 |
| 数据检查 | 50 个城市编码唯一；2 条来源于截图，48 条为本地模拟；定向审阅确认已配置编码和别名均命中相应城市 |
| 真实 HTTP | 50 条城市名称逐一查询通过；深圳北站、成都天府机场、汉口站、昆山、桐庐和无匹配结果均符合预期 |
| 页面操作 | 0.1.1 调试页只展示 filter；输入“深圳北站”并执行后，HTTP 200、code=SUCCESS、cityId=DEMO_SHENZHEN |
| 文档检查 | 代码、OpenAPI、README、示例与 V0.2 对接资料中的过时查询分支、参数及说明已清理 |

服务已使用原数据库重启，地址仍为 http://127.0.0.1:8766/docs。本次运行验证只查询城市，没有新增模拟申请或修改场景配置。

## 2026-09-12 Dify API 验证

新增 `mock_travel/dify_probe.py`，支持仅网络、应用鉴权和两轮会话三种模式。最初 7 项新增测试先因实现缺失失败，补齐实现后通过。定向审阅发现异常格式密钥在转义后可能绕过脱敏，增加 3 项回归测试，确认先失败后修复：请求前拒绝非打印字符，结果在 JSON 序列化前递归脱敏，网络异常仅展示异常类别。最终完整测试套件运行结果为 `33 passed in 0.66s`，包含原后端 23 项和 Dify 检查 10 项。

真实网络检查于北京时间 2026-09-12 16:48 执行：不带 Authorization 请求 `https://api.dify.ai/v1/parameters` 返回 HTTP 401，脚本明确记录 `network=reachable`、`authentication=not_tested`，退出码为 0。报告保存在本目录 `.local/dify-check-result.json`。

Chrome 当前登录可用，目标应用「差旅申请助手-MVP1.2-测试」的访问点显示后端服务 API 为「服务中」。最初查看密钥弹窗时数量为 0；随后用户提供应用密钥，已写入 `.local/dify.json`，权限为 `0600`。本次没有创建新密钥、修改线上工作流或公开本地服务。

北京时间 17:22 开始的真实鉴权检查通过：`GET /info` 和 `GET /parameters` 均 HTTP 200，应用名称准确匹配。17:23 开始的两轮 `POST /chat-messages` 均 HTTP 200，返回相同 `conversation_id`：`e9b15f44-9e8f-4ab8-80d2-f19303bc4ac9`。

人工核对两轮回复：首轮生成 9 月 13 日杭州到上海、9 月 14 日上海返杭州的草稿，事由为“接口连通性测试”；第二轮改成“接口连通性复测”，行程与日期保持正确。本次没有发送确认提交指令。最终报告覆盖为 `mode=chat`、`authentication=passed`、`conversation=passed`，包含脱敏后的两轮回复。

当前通过的是本地调用 Dify API 及该两轮草稿会话，未验证 Dify Cloud 调用本地模拟接口、模拟创建成功／失败或完整业务链路。现有 Chatflow 仍包含原 Demo 数据及旧校验提示，留待接口接入阶段按新范围同步。

详细操作与验收范围见 [Dify API 验证说明](/Users/khalil/Documents/智能提单项目/模拟差旅系统/Dify_API验证说明.md)。
