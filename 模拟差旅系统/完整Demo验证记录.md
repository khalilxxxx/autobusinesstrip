# 完整 Demo 验证记录

后续草稿卡片与编辑抽屉升级的验证见 [草稿交互验证记录](草稿交互验证记录.md)。本页保留首次 Demo 接入时的历史结果，其中「确认提交」和失败后重试流程以最新使用说明为准。

2026-09-12。本记录来自实际 Dify Cloud 与本地页面/API 联调。按用户最新要求，仅验证两份草稿的必要流程，未开展批量用户模拟测试；后续批量测试由用户使用 Luna 执行。

## 已交付与运行状态

- 助手与模拟控制台由 `127.0.0.1:8766` 托管，受限网关监听 `127.0.0.1:8767`，后台通道运行中。
- 已从当前 Dify 测试应用导出当日基线；保留原 MVP1.2 包。
- Dify 测试应用 `9f14a8eb-d849-41cb-8dbd-b66829b17edb` 已导入 39 节点、69 连线的接入版，平台检查清单显示「所有问题均已解决」，发布版本为 **#3**。
- 公开 DSL SHA256：`e7b75d0b53639c8f1ea7ec8af40a8dcf129cc0616b4a6e0cc067abd4a8e13a55`。

## 实际联调结果

| 场景 | 实际结果 |
|---|---|
| 信息不完整 | 输入杭州东站→深圳北站拜访客户；工作流真实调用员工/交通/城市接口，记录草稿并追问日期和返程 |
| 补充行程 | 10月18日去深圳、20日回杭州，往返飞机经济舱；生成两段确认单，状态 READY_TO_CONFIRM、revision 2 |
| 修改事由 | 改为「深圳项目验收」，往返城市/日期/交通保持不变；确认版本升至 revision 3 |
| 页面确认成功 | 从浏览器点击「确认提交」，真实创建 `DEMO-CL-20260912-4A393598D9A8`；助手、详情、回执一致 |
| 防重复 | 页面再次发送「确认提交」后回放原单号；申请总数维持 3，没有第二次创建调用 |
| 首轮显示与刷新 | 从页面新建杭州→桐庐当天往返草稿，首轮消息和处理中提示可见；处理中刷新后恢复同一任务并取得确认单 |
| 历史会话 | 页面切到已提交深圳会话再返回桐庐草稿，两份内容与各自状态正确保留 |
| 预设失败 | 从控制台设为失败并填写说明；原草稿确认后显示「演示：差旅系统暂时不可用，请稍后重试。」；申请总数仍为 3，回执 FAILED |
| 失败后主动重试 | 控制台恢复成功，在原失败会话再次确认；创建 `DEMO-CL-20260912-112469530D48`，申请总数变为 4 |
| 回执保留 | 重试使用新请求号并返回 SUCCEEDED；原请求号仍查询到 FAILED；草稿 ID、revision 与指纹保持一致 |

演示前已有两张历史模拟申请，保留未删。此轮联调只新增上述两张成功申请。结束时场景为 SUCCESS。

失败请求号：`DEMO-REQ-a49ec050139ab5bd0dddca9c4ed10e8faad058b1`。

重试成功请求号：`DEMO-REQ-6792e6bda681574b0d6e03493c0e60d1912408d4`。

完整回复、整理后的会话状态、单据、回执与 DIFY 来源调用记录保存在 [少量真实联调记录.json](/Users/khalil/Documents/智能提单项目/交付物/完整Demo_Dify接入/验收/少量真实联调记录.json)。不包含 Dify 密钥或网关凭证。

## 界面与连接检查

Chrome 实际检查了浅紫双栏助手、Markdown 行程表格、确认按钮、处理中禁发、提交结果和详情跳转；同时检查了控制台的场景切换、单据详情和调用列表。已在页面确认真实成功、失败与重试成功文案，非静态占位。

公网通道实测：未带凭证 401、授权上下文请求 200、助手 API 路径 404。回执适配以 HTTP 200 携带原接口的 404 状态，原模拟回执路由保持 404；查不到不被当作业务失败。

已经复核 `run-demo.sh` 启动/复用、`restart-local` 保留通道与地址。脚本仅按保存的 PID、启动时间和命令停止本 Demo 的进程；不会按端口结束未知程序。

## 必要自动检查与审阅

- 前端最终 12 项组件/逻辑定向回归通过，TypeScript/Vite 生产构建通过；修复了空待办误锁、首轮消息遮挡、错会话写入、轮询重连、异常提示和详情竞态。
- 最终运行助手代理、网关与工作流适配相关的 15 项后端检查，全部通过。此前原模拟服务与 Dify 探针套件已有通过记录。
- 工作流保留的 44 项离线验证已通过；这些夹具验证不替代上述真实联调。
- 工作流、前端及启动脚本完成定向独立审阅，无剩余必修项。
- 最终公开前端产物与工作流文件未包含当前真实凭证；Dify 配置、网关配置及本机运行 DSL 权限均为 `0600`。

## 使用限制

所有单据均为本地模拟数据，没有调用公司内网。前端申请详情目前保留原始城市/员工/组织编码，助手确认单使用城市名称。临时通道重启会更换地址，需要同步 Dify 的 `DEMO_API_BASE_URL` 并发布；启动与维护步骤见 [完整 Demo 使用说明](/Users/khalil/Documents/智能提单项目/模拟差旅系统/完整Demo使用说明.md)。

网络中断的状态保护、旧版本确认及错误分支已由定向检查覆盖；本次未做大范围真实故障注入、压力测试或批量会话测试。
